# ✅ 故障排查报告

## 📋 问题总结

**原因**: 系统缺少 Python 依赖包（`streamlit`、`openai` 等）

**不是 API Key 的问题！** 这是依赖安装问题。

---

## ✅ 已解决的问题

### 1. 安装了核心依赖
- ✅ streamlit (Web 框架)
- ✅ openai (LLM API 客户端)
- ✅ httpx, aiohttp (HTTP 客户端)
- ✅ aiofiles (异步文件操作)

### 2. 主应用已启动
- ✅ Flask 主应用运行在 http://localhost:5000
- ✅ 状态码: 200 (正常)
- ✅ 浏览器已自动打开

---

## ⚠️ 仍需解决

### 可能还缺少的依赖包

由于 `requirements.txt` 中包含需要编译的包（如 `matplotlib`、`playwright` 等），
这些包在没有 Visual Studio 编译环境的 Windows 上无法安装。

### 解决方案

**方法 1: 逐步安装（推荐）** ⭐

1. **先配置 API Key**（最重要！）
   
   编辑 `.env` 文件，至少配置这 3 个：
   ```env
   # 方案 A: 使用 AIHubMix 统一密钥
   INSIGHT_ENGINE_API_KEY=sk-your-aihubmix-key
   INSIGHT_ENGINE_BASE_URL=https://aihubmix.com/v1
   INSIGHT_ENGINE_MODEL_NAME=gpt-4o-mini
   
   QUERY_ENGINE_API_KEY=sk-your-aihubmix-key
   QUERY_ENGINE_BASE_URL=https://aihubmix.com/v1
   QUERY_ENGINE_MODEL_NAME=gpt-4o-mini
   
   REPORT_ENGINE_API_KEY=sk-your-aihubmix-key
   REPORT_ENGINE_BASE_URL=https://aihubmix.com/v1
   REPORT_ENGINE_MODEL_NAME=gemini-2.0-flash-exp
   ```
   
   或者
   
   ```env
   # 方案 B: 使用 DeepSeek（免费 500 万 tokens）
   INSIGHT_ENGINE_API_KEY=sk-your-deepseek-key
   INSIGHT_ENGINE_BASE_URL=https://api.deepseek.com
   INSIGHT_ENGINE_MODEL_NAME=deepseek-chat
   
   QUERY_ENGINE_API_KEY=sk-your-deepseek-key
   QUERY_ENGINE_BASE_URL=https://api.deepseek.com
   QUERY_ENGINE_MODEL_NAME=deepseek-chat
   
   REPORT_ENGINE_API_KEY=sk-your-deepseek-key
   REPORT_ENGINE_BASE_URL=https://api.deepseek.com
   REPORT_ENGINE_MODEL_NAME=deepseek-chat
   ```

2. **尝试使用应用**
   
   访问 http://localhost:5000，尝试使用各个功能。
   
3. **按需安装依赖**
   
   如果遇到 "No module named xxx" 错误，运行：
   ```bash
   python -m pip install xxx --only-binary=:all:
   ```
   
   常见可能缺少的包：
   - `tavily-python` (Tavily 搜索)
   - `playwright` (网页抓取)
   - `jieba` (中文分词)
   - `pymysql` (MySQL 数据库，如果使用)
   - `redis` (Redis 缓存，如果使用)

**方法 2: 跳过问题包，安装其他依赖**

运行以下命令，跳过需要编译的包：

```bash
python -m pip install -r requirements.txt --only-binary=:all: || echo "部分包安装失败，继续..."
```

这会尽可能多地安装包，跳过无法安装的。

**方法 3: 安装 Visual Studio Build Tools**（如需完整功能）

如果你需要使用所有功能（包括图表生成、网页抓取等），需要安装编译工具：

1. 下载 Visual Studio Build Tools:
   https://visualstudio.microsoft.com/zh-hans/downloads/
   
2. 安装时选择 "C++ 生成工具"

3. 然后重新运行：
   ```bash
   python -m pip install -r requirements.txt
   ```

---

## 🎯 当前状态

### ✅ 正常运行的部分
- Flask 主应用
- Web 界面访问
- 基础路由

### ⚠️ 可能无法使用的功能
- Streamlit 子应用（如果还缺少依赖）
- 图表生成（matplotlib 未安装）
- 网页抓取（playwright 未安装）
- 某些数据库功能（如果未配置）

---

## 📱 测试步骤

1. **打开浏览器**: http://localhost:5000

2. **查看界面**:
   - 如果看到正常的主页 ✅ → 继续
   - 如果显示错误 ❌ → 查看错误信息，按需安装依赖

3. **点击功能模块**:
   - Insight Engine
   - Query Engine  
   - Media Engine
   - Report Engine

4. **如果某个模块启动失败**:
   - 查看日志文件: `logs\xxx.log`
   - 找到 "No module named xxx" 错误
   - 运行: `python -m pip install xxx`
   - 重启应用

---

## 🔑 配置 API Key（必做）

### 快速配置

**如果你用 AIHubMix**:

1. 注册: https://aihubmix.com/?aff=8Ds9
2. 获取 API Key
3. 复制 `AIHubMix配置示例.txt` 内容到 `.env`
4. 替换所有 `sk-your-aihubmix-key-here` 为你的真实密钥

**如果你用 DeepSeek**:

1. 注册: https://platform.deepseek.com/
2. 获取 API Key（送 500 万 tokens）
3. 在 `.env` 中配置:
   ```env
   INSIGHT_ENGINE_API_KEY=sk-你的deepseek密钥
   INSIGHT_ENGINE_BASE_URL=https://api.deepseek.com
   INSIGHT_ENGINE_MODEL_NAME=deepseek-chat
   
   QUERY_ENGINE_API_KEY=sk-你的deepseek密钥
   QUERY_ENGINE_BASE_URL=https://api.deepseek.com
   QUERY_ENGINE_MODEL_NAME=deepseek-chat
   
   REPORT_ENGINE_API_KEY=sk-你的deepseek密钥
   REPORT_ENGINE_BASE_URL=https://api.deepseek.com
   REPORT_ENGINE_MODEL_NAME=deepseek-chat
   ```

### 重启应用

配置完成后，重启应用：
```bash
npm run dev
```

或者双击 `启动应用.bat`

---

## 💡 常见问题

### Q1: 前台显示"系统启动失败"？

**A**: 有以下几种可能：

1. **缺少依赖包**
   - 查看日志: `logs\insight.log`、`logs\query.log`
   - 安装缺少的包

2. **未配置 API Key**
   - 编辑 `.env` 文件
   - 填写有效的 API Key

3. **API Key 无效**
   - 检查密钥是否正确
   - 确认账户有额度

### Q2: 某个 Engine 无法启动？

**A**: 

1. 查看对应的日志文件
2. 如果是 "No module named xxx"，安装该模块
3. 如果是 API 错误，检查 `.env` 中的配置
4. 重启应用

### Q3: 安装依赖时总是失败？

**A**: 

Windows 下某些包需要编译器。解决方案：

1. **使用预编译版本**（推荐）:
   ```bash
   python -m pip install xxx --only-binary=:all:
   ```

2. **跳过该功能**，使用其他替代方案

3. **安装 Visual Studio Build Tools**

### Q4: 应用启动很慢？

**A**: 

正常现象。Streamlit 子应用需要：
- 导入大量库
- 初始化 LLM 客户端
- 启动多个进程

第一次启动可能需要 30-60 秒。

---

## 🔍 日志位置

所有日志文件在 `logs/` 目录：

- `insight.log` - Insight Engine 日志
- `query.log` - Query Engine 日志
- `media.log` - Media Engine 日志
- `forum.log` - Forum Engine 日志

查看日志命令：
```bash
# PowerShell
Get-Content logs\insight.log | Select-Object -Last 20

# CMD
type logs\insight.log | more
```

---

## 📞 需要帮助？

1. **查看配置指南**: `配置指南.md`
2. **查看 API 方案对比**: `API配置方案对比.md`
3. **查看快速开始**: `快速开始.md`
4. **GitHub Issues**: https://github.com/666ghj/BettaFish/issues
5. **项目 README**: README.md

---

## 🎉 总结

### 当前状态
- ✅ 主要依赖已安装 (streamlit, openai)
- ✅ Flask 主应用运行正常
- ✅ 浏览器已打开应用
- ⏳ 需要配置 API Key
- ⚠️ 可能还缺少部分可选依赖

### 下一步（3 步完成）
1. ✏️ 编辑 `.env` 文件，填写 API Key
2. 🔄 重启应用: `npm run dev`
3. 🎊 开始使用！

**最快配置方案**: 注册 DeepSeek（5 分钟，免费 500 万 tokens） → 配置到 `.env` → 重启 → 完成！

祝你使用愉快！🚀

