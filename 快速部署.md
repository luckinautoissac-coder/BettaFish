# 🚀 快速部署到云端

## ⚠️ 重要提示

**BettaFish 不适合部署到 Vercel！** 

Vercel 是 Serverless 平台，不支持：
- ❌ 长期运行的进程（Flask + Streamlit）
- ❌ WebSocket 持久连接
- ❌ 多个子进程

**推荐平台**: Railway 或 Render

---

## 📋 准备工作（5 分钟）

### 第 1 步: 推送到 GitHub

```bash
# 在项目目录打开终端
cd "E:\5. Betta Fish\BettaFish"

# 查看状态
git status

# 添加所有文件（.env 会自动被忽略）
git add .

# 提交
git commit -m "feat: 添加部署配置文件"

# 推送到 GitHub
git push
```

⚠️ **检查**: 确保 `.env` 文件**没有**被推送！

---

## 🚂 方案 A: Railway 部署（推荐）⭐⭐⭐⭐⭐

### 为什么选 Railway？
- ✅ 最适合 Python 长进程应用
- ✅ $5/月 免费额度
- ✅ 自动从 GitHub 部署
- ✅ 配置最简单

### 部署步骤（10 分钟）

#### 1. 注册 Railway
访问: https://railway.app/
点击 "Login with GitHub"

#### 2. 创建新项目
- 点击 "New Project"
- 选择 "Deploy from GitHub repo"
- 选择你的 BettaFish 仓库
- Railway 会自动检测配置

#### 3. 添加环境变量

在 Railway 项目设置 → Variables 中添加：

```env
# 复制你的 .env 文件内容，粘贴到这里

# 必须配置的：
HOST=0.0.0.0
PORT=5000

# API Keys（使用你的真实密钥）
INSIGHT_ENGINE_API_KEY=sk-你的密钥
INSIGHT_ENGINE_BASE_URL=https://api.deepseek.com
INSIGHT_ENGINE_MODEL_NAME=deepseek-chat

QUERY_ENGINE_API_KEY=sk-你的密钥
QUERY_ENGINE_BASE_URL=https://api.deepseek.com
QUERY_ENGINE_MODEL_NAME=deepseek-chat

REPORT_ENGINE_API_KEY=sk-你的密钥
REPORT_ENGINE_BASE_URL=https://api.deepseek.com
REPORT_ENGINE_MODEL_NAME=deepseek-chat

# 可选的其他配置...
```

💡 **提示**: 直接复制你本地 `.env` 文件的内容，粘贴到 Railway 的环境变量中。

#### 4. 部署

Railway 会自动：
- 安装依赖
- 启动应用
- 分配域名

#### 5. 访问应用

部署完成后，Railway 会提供一个 URL：
```
https://你的应用名.railway.app
```

点击访问即可！

### 监控和日志

- **查看日志**: Railway Dashboard → Logs
- **查看指标**: Railway Dashboard → Metrics
- **重新部署**: Railway Dashboard → Deploy → Redeploy

---

## 🎨 方案 B: Render 部署（免费）

### 特点
- ✅ 完全免费
- ⚠️ 15 分钟无活动会休眠
- ⚠️ 冷启动较慢（1-2 分钟）

### 部署步骤（10 分钟）

#### 1. 注册 Render
访问: https://render.com/
点击 "Get Started" 用 GitHub 登录

#### 2. 创建 Web Service
- 点击 "New +" → "Web Service"
- 选择你的 BettaFish 仓库
- Render 会检测到 `render.yaml`

#### 3. 配置环境变量

在 Environment 标签页添加：
```env
HOST=0.0.0.0
PORT=10000
INSIGHT_ENGINE_API_KEY=你的密钥
INSIGHT_ENGINE_BASE_URL=https://api.deepseek.com
INSIGHT_ENGINE_MODEL_NAME=deepseek-chat
# ... 其他环境变量
```

#### 4. 部署

点击 "Create Web Service"，等待部署完成（5-10 分钟）。

#### 5. 访问应用

```
https://你的应用名.onrender.com
```

### ⚠️ Render 免费版限制

- 每月 750 小时免费
- 15 分钟不活动会休眠
- 内存限制 512MB
- 冷启动 1-2 分钟

---

## 🐳 方案 C: Docker 部署（VPS）

如果你有自己的服务器（VPS）：

### 在服务器上运行

```bash
# SSH 连接到服务器
ssh root@你的服务器IP

# 安装 Docker
curl -fsSL https://get.docker.com | sh

# 克隆仓库
git clone https://github.com/你的用户名/BettaFish.git
cd BettaFish

# 创建 .env 文件
nano .env
# 粘贴你的环境变量，保存退出

# 启动
docker-compose up -d

# 查看日志
docker-compose logs -f
```

### 访问

```
http://你的服务器IP:5000
```

### 管理命令

```bash
# 停止
docker-compose down

# 重启
docker-compose restart

# 查看状态
docker-compose ps

# 更新代码
git pull
docker-compose up -d --build
```

---

## ❌ 为什么不推荐 Vercel？

### Vercel 的限制

| 特性 | Vercel | BettaFish 需求 |
|------|--------|--------------|
| 执行时间 | 最长 60 秒 | ❌ 分析可能需要几分钟 |
| 长进程 | 不支持 | ❌ 需要 Flask + Streamlit |
| WebSocket | 不支持 | ❌ 需要实时通信 |
| 子进程 | 不支持 | ❌ 需要多个 Streamlit 实例 |

### 如果必须用 Vercel

**只能部署简化版本**，去掉：
- ❌ 所有 Streamlit 功能
- ❌ 长时间运行的分析任务
- ❌ 实时更新
- ❌ 多个引擎

**结果**: 失去 80% 功能，不建议！

---

## 📊 平台对比

| 特性 | Railway | Render | Vercel | VPS |
|------|---------|--------|--------|-----|
| **价格** | $5/月 免费额度 | 完全免费 | 免费 | $5-10/月 |
| **部署难度** | ⭐⭐ | ⭐⭐ | ⭐ | ⭐⭐⭐⭐ |
| **适配性** | ✅ 完美 | ✅ 完美 | ❌ 不支持 | ✅ 完美 |
| **稳定性** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | - | ⭐⭐⭐⭐⭐ |
| **冷启动** | 无 | 15 分钟后休眠 | - | 无 |
| **推荐度** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ❌ | ⭐⭐⭐⭐⭐ |

### 推荐选择

- **开发/测试**: Render（免费）
- **生产环境**: Railway（性价比高）
- **高流量**: 自己的 VPS（完全控制）
- ~~个人博客~~: ~~Vercel~~（不适合 BettaFish）

---

## 🐛 常见问题

### Q1: 推送到 GitHub 时显示 .env？

**A**: 
```bash
# 确保 .gitignore 包含 .env
echo ".env" >> .gitignore

# 如果已经提交了 .env，移除它：
git rm --cached .env
git commit -m "chore: 移除敏感文件"
git push
```

### Q2: Railway/Render 部署失败？

**A**: 常见原因：
1. 缺少环境变量 → 检查是否配置了所有 API Keys
2. 内存不足 → 升级到付费计划或使用 VPS
3. 依赖安装失败 → 检查 `requirements.txt`

**查看日志**:
- Railway: Dashboard → Logs
- Render: Dashboard → Logs

### Q3: 应用启动很慢？

**A**: 
- **首次部署**: 需要安装依赖（5-10 分钟）
- **Render 冷启动**: 休眠后需要 1-2 分钟启动
- **Railway**: 无冷启动

**优化建议**:
- 使用 Railway（$5/月，无休眠）
- 使用 UptimeRobot 定期 ping（防止 Render 休眠）

### Q4: 能同时部署到多个平台吗？

**A**: 可以！
- Railway: 生产环境
- Render: 测试环境
- VPS: 备份环境

每个平台独立配置，互不影响。

### Q5: 成本问题？

**A**: 
- **完全免费**: Render（但会休眠）
- **性价比**: Railway $5/月（推荐）
- **完全控制**: VPS $5-10/月（Vultr/DigitalOcean）

---

## 📞 需要帮助？

- **详细部署指南**: 查看 `部署指南.md`
- **Railway 文档**: https://docs.railway.app/
- **Render 文档**: https://render.com/docs
- **项目 GitHub**: https://github.com/666ghj/BettaFish

---

## ✅ 部署清单

### 推送前

- [ ] 确认 `.env` 在 `.gitignore` 中
- [ ] 本地测试通过
- [ ] 提交所有更改
- [ ] 推送到 GitHub

### Railway 部署

- [ ] 注册 Railway
- [ ] 连接 GitHub 仓库
- [ ] 配置环境变量（复制 .env 内容）
- [ ] 等待部署完成
- [ ] 测试应用

### Render 部署

- [ ] 注册 Render
- [ ] 选择 Web Service
- [ ] 连接 GitHub 仓库
- [ ] 配置环境变量
- [ ] 创建服务
- [ ] 等待部署（5-10 分钟）
- [ ] 测试应用

---

## 🎉 快速命令参考

### Git 推送
```bash
git add .
git commit -m "feat: 添加部署配置"
git push
```

### 查看部署日志
- **Railway**: Dashboard → Logs
- **Render**: Dashboard → Logs
- **Docker**: `docker-compose logs -f`

### 重新部署
- **Railway**: Dashboard → Deploy → Redeploy
- **Render**: 自动触发（push 后）
- **Docker**: `docker-compose up -d --build`

---

## 🚀 最快部署路径（15 分钟）

1. ✅ `git push` 推送到 GitHub（2 分钟）
2. ✅ 注册 Railway（1 分钟）
3. ✅ 连接 GitHub 仓库（1 分钟）
4. ✅ 复制 `.env` 内容到环境变量（5 分钟）
5. ✅ 等待部署完成（5 分钟）
6. ✅ 访问你的应用！

**总时间**: 约 15 分钟
**成本**: $5/月（Railway）或 免费（Render）

祝你部署成功！🎊

