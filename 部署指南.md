# 🚀 BettaFish 部署指南

## 📋 目录

1. [推送到 GitHub](#1-推送到-github)
2. [推荐部署平台](#2-推荐部署平台)
3. [Railway 部署（推荐）](#3-railway-部署推荐)
4. [Render 部署](#4-render-部署)
5. [Docker 部署](#5-docker-部署)
6. [Vercel 部署（不推荐）](#6-vercel-部署不推荐)

---

## 1. 推送到 GitHub

### 步骤 1: 检查 Git 状态

打开终端/PowerShell，运行：

```bash
cd "E:\5. Betta Fish\BettaFish"
git status
```

### 步骤 2: 添加 .gitignore

确保 `.env` 文件不会被推送（包含密钥，不能公开）：

```bash
# 检查 .gitignore 是否存在
cat .gitignore
```

`.gitignore` 应该包含：
```
.env
*.pyc
__pycache__/
logs/
*.log
node_modules/
venv/
.venv/
```

### 步骤 3: 提交本地更改

```bash
# 添加所有文件
git add .

# 提交（但排除 .env）
git commit -m "chore: 添加配置文件和启动脚本

- 添加 package.json 支持 npm 命令
- 添加配置文档和示例
- 添加 Windows 启动脚本
- 安装核心依赖
"
```

### 步骤 4: 推送到 GitHub

**如果还没有关联远程仓库：**

```bash
# 创建新仓库后（在 GitHub 网站上操作）
git remote add origin https://github.com/你的用户名/BettaFish.git
git branch -M main
git push -u origin main
```

**如果已经关联了：**

```bash
git push
```

### 步骤 5: 验证推送

访问你的 GitHub 仓库页面，确认文件已上传。

⚠️ **重要**: 确保 `.env` 文件**没有**被推送上去！

---

## 2. 推荐部署平台

| 平台 | 免费额度 | 难度 | 推荐度 | 适合场景 |
|------|---------|------|-------|---------|
| **Railway** | $5/月免费 | ⭐⭐ | ⭐⭐⭐⭐⭐ | 最佳选择，支持长进程 |
| **Render** | 512MB 免费 | ⭐⭐ | ⭐⭐⭐⭐ | 免费方案，但会休眠 |
| **Fly.io** | 3 个免费应用 | ⭐⭐⭐ | ⭐⭐⭐⭐ | 支持 Docker |
| **Heroku** | 无免费额度 | ⭐⭐ | ⭐⭐⭐ | 需付费 |
| **自己的服务器** | 取决于 VPS | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 完全控制 |
| **Vercel** | 免费 | ⭐ | ⭐ | ❌ 不推荐 |

---

## 3. Railway 部署（推荐）⭐⭐⭐⭐⭐

Railway 是最适合 BettaFish 的平台。

### 为什么选 Railway？
- ✅ 支持长期运行的进程
- ✅ 支持 Python + Flask + Streamlit
- ✅ 自动从 GitHub 部署
- ✅ 提供 $5/月 免费额度
- ✅ 配置简单

### 部署步骤

#### 第 1 步: 注册 Railway
访问: https://railway.app/
使用 GitHub 账号登录

#### 第 2 步: 创建项目文件

在项目根目录创建 `railway.json`:

```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python app.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

创建 `Procfile`:
```
web: python app.py
```

#### 第 3 步: 推送到 GitHub

```bash
git add railway.json Procfile
git commit -m "feat: 添加 Railway 部署配置"
git push
```

#### 第 4 步: 在 Railway 创建项目

1. 点击 "New Project"
2. 选择 "Deploy from GitHub repo"
3. 选择你的 BettaFish 仓库
4. Railway 会自动检测为 Python 项目

#### 第 5 步: 配置环境变量

在 Railway 项目设置中添加环境变量：

```
HOST=0.0.0.0
PORT=5000
INSIGHT_ENGINE_API_KEY=你的密钥
INSIGHT_ENGINE_BASE_URL=https://api.deepseek.com
INSIGHT_ENGINE_MODEL_NAME=deepseek-chat
QUERY_ENGINE_API_KEY=你的密钥
QUERY_ENGINE_BASE_URL=https://api.deepseek.com
QUERY_ENGINE_MODEL_NAME=deepseek-chat
REPORT_ENGINE_API_KEY=你的密钥
REPORT_ENGINE_BASE_URL=https://api.deepseek.com
REPORT_ENGINE_MODEL_NAME=deepseek-chat
```

#### 第 6 步: 部署

Railway 会自动构建和部署。
完成后会提供一个 URL，例如：`https://你的应用.railway.app`

#### 第 7 步: 监控

在 Railway Dashboard 查看：
- 部署日志
- 应用状态
- 资源使用

---

## 4. Render 部署

Render 提供免费套餐，但应用会在 15 分钟不活动后休眠。

### 部署步骤

#### 第 1 步: 注册 Render
访问: https://render.com/
使用 GitHub 账号登录

#### 第 2 步: 创建 render.yaml

在项目根目录创建 `render.yaml`:

```yaml
services:
  - type: web
    name: bettafish
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: 5000
      - key: INSIGHT_ENGINE_API_KEY
        sync: false
      - key: QUERY_ENGINE_API_KEY
        sync: false
      - key: REPORT_ENGINE_API_KEY
        sync: false
```

#### 第 3 步: 推送到 GitHub

```bash
git add render.yaml
git commit -m "feat: 添加 Render 部署配置"
git push
```

#### 第 4 步: 在 Render 创建服务

1. 点击 "New +" → "Web Service"
2. 连接你的 GitHub 仓库
3. 选择 BettaFish 仓库
4. Render 会自动检测 `render.yaml`

#### 第 5 步: 配置环境变量

添加你的 API Keys（与 Railway 相同）

#### 第 6 步: 部署

点击 "Create Web Service"，Render 会自动部署。

---

## 5. Docker 部署

如果你有自己的服务器（VPS），可以使用 Docker。

### 步骤

项目已经包含 `Dockerfile` 和 `docker-compose.yml`。

#### 在服务器上运行：

```bash
# 克隆仓库
git clone https://github.com/你的用户名/BettaFish.git
cd BettaFish

# 创建 .env 文件并填写密钥
nano .env

# 启动
docker-compose up -d

# 查看日志
docker-compose logs -f
```

#### 访问应用

```
http://你的服务器IP:5000
```

---

## 6. Vercel 部署（不推荐）❌

### 为什么不推荐？

Vercel 的限制：
- ❌ Serverless 函数最长执行 60 秒（免费）/ 300 秒（付费）
- ❌ 不支持长期运行的进程
- ❌ 不支持 WebSocket（长连接）
- ❌ 不支持多个子进程
- ❌ Streamlit 无法运行

### 如果坚持使用 Vercel（仅主页面）

**只能部署 Flask 主应用的静态部分，无法使用 Streamlit 功能。**

#### 创建 vercel.json

```json
{
  "version": 2,
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
```

#### 修改 app.py

需要大幅修改代码，移除所有 Streamlit 相关功能，只保留基础 Flask 路由。

**结论**: 这会失去 BettaFish 80% 的功能，不建议这样做。

---

## 📊 平台对比总结

### 如果你想要...

- **🆓 免费 + 简单**: → **Render**（但会休眠）
- **⚡ 最佳性能 + 稳定**: → **Railway**（$5/月免费额度）
- **🎛️ 完全控制**: → **自己的服务器 + Docker**
- **🚀 快速测试**: → **Railway** 或 **Render**

### 推荐顺序

1. **Railway** ⭐⭐⭐⭐⭐ （首选）
2. **Render** ⭐⭐⭐⭐ （免费，但会休眠）
3. **Fly.io** ⭐⭐⭐⭐ （需要信用卡）
4. **自己的 VPS** ⭐⭐⭐⭐⭐ （完全控制）
5. ~~Vercel~~ ❌ （不推荐）

---

## 🔧 部署前检查清单

### 代码检查

- [ ] `.gitignore` 包含 `.env`
- [ ] 推送到 GitHub 成功
- [ ] `.env` 没有被推送
- [ ] `requirements.txt` 存在
- [ ] `Dockerfile` 存在（如用 Docker）

### 环境变量准备

准备好以下环境变量：

```
HOST=0.0.0.0
PORT=5000
INSIGHT_ENGINE_API_KEY=***
INSIGHT_ENGINE_BASE_URL=***
INSIGHT_ENGINE_MODEL_NAME=***
QUERY_ENGINE_API_KEY=***
QUERY_ENGINE_BASE_URL=***
QUERY_ENGINE_MODEL_NAME=***
REPORT_ENGINE_API_KEY=***
REPORT_ENGINE_BASE_URL=***
REPORT_ENGINE_MODEL_NAME=***
```

### 数据库配置（可选）

如果使用数据库功能：

```
DB_DIALECT=postgresql
DB_HOST=***
DB_PORT=5432
DB_USER=***
DB_PASSWORD=***
DB_NAME=***
```

---

## 🐛 常见部署问题

### Q1: 部署后应用启动失败？

**A**: 查看部署日志：
- Railway: Dashboard → Logs
- Render: Dashboard → Logs
- 通常是缺少依赖或环境变量

### Q2: 应用启动但无法访问？

**A**: 检查：
1. `HOST` 必须是 `0.0.0.0`（不是 `localhost`）
2. `PORT` 使用平台提供的（Railway/Render 会自动设置）
3. 防火墙设置

### Q3: Streamlit 子应用无法启动？

**A**: 
- 确保平台支持长期运行的进程
- 检查内存限制（至少 512MB）
- 查看日志中的错误信息

### Q4: 成本问题？

**A**: 推荐方案：
- **开发/测试**: Render 免费套餐
- **生产环境**: Railway ($5-$10/月)
- **高流量**: 自己的 VPS (Vultr/DigitalOcean, $5-$10/月)

---

## 📞 需要帮助？

- **Railway 文档**: https://docs.railway.app/
- **Render 文档**: https://render.com/docs
- **BettaFish GitHub**: https://github.com/666ghj/BettaFish/issues

---

## 🎉 快速开始

### 最简单的方式（推荐）:

1. ✅ 代码推送到 GitHub
2. ✅ 注册 Railway: https://railway.app/
3. ✅ 创建 `railway.json` 和 `Procfile`
4. ✅ 从 GitHub 导入项目
5. ✅ 添加环境变量
6. ✅ 部署完成！

**预计时间**: 15 分钟

祝你部署成功！🚀

